import { Typography } from "@mui/material";


const Logout = () => {
  return (
    <>
      <Typography mb={2} fontFamily={'Poppins, sans-serif'} fontWeight={699} variant="body1" gutterBottom>Logout</Typography>
      <Typography fontSize={'12px'} fontFamily={'Poppins, sans-serif'} > Logout page</Typography>  
    </>
  );
};

export default Logout;
